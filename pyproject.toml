[project]
name = "deriv-assessment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nishal", email = "nishalahmed05@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "google-genai>=2.25.0",
    "pydantic>=2.13.5",
]

[project.scripts]
deriv-assessment = "deriv_assessment:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
